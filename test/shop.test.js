const Shop = require("../src/shop");

// Future: use beforeEach() to reduce code unnecessary code repetition

describe("Item", () => {
  describe("updateQuality()", () => {
    it("", () => {
      shop = new Shop([]);

      expect(shop.updateItemQuality()).toEqual([]);
    });
  });
});
